// Phase 4: Investments tab
// TODO: Investment positions table — support manual entry of individual holdings
// (VTI, NVDA, etc.) and eventual brokerage sync for Holdings mix by asset class view.
// Currently shows account-level balances only (no position data in backend).

export default function Investments() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold" style={{ color: "var(--text)" }}>
        Investments
      </h1>
      <p className="mt-2 text-sm" style={{ color: "var(--muted)" }}>
        Coming in Phase 4
      </p>
    </div>
  )
}
